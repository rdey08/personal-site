# Drafts (parked content)

Content waiting on facts only Rupak can supply. Files here are outside the
build (only content/ ships) and outside the check-facts scan roots, so their
VERIFY markers do not block CI. When the facts are confirmed, fill them in
and move the file back to the matching content/ directory.
